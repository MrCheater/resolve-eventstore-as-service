{
  "name": "eventstore-as-service",
  "version": "0.0.1",
  "license": "MIT",
  "scripts": {
    "prepare": "tsc",
    "build": "build-assets --name=eventstore-as-service",
    "test": "echo || exit 0",
    "test:w": "echo || exit 0",
    "test:c": "echo || exit 0",
    "test:f": "echo || exit 0"
  },
  "dependencies": {
    "pg": "8.7.1"
  }
}
